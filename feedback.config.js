/* REAL feedback config — gitignored (not in the public source repo).
   Deployed to the gh-pages branch so the live site can reach the Worker.
   A private backup lives in ~/keys/qutip-sandbox/. */
window.QIF_FEEDBACK = {
  enabled: true,
  endpoint: "https://qutip-feedback.dev-fa0.workers.dev",
};
