qutip>=5.3
matplotlib>=3.11

# Optional: enriches the math-equation -> speech fallback (scripts/mathspeech.py).
# Without it, the built-in rule-based translator is used. Speech Rule Engine
# (highest quality, MathML input) is reached via `npx speech-rule-engine` and
# needs Node, not pip.
pylatexenc>=2.10
pytest>=8
